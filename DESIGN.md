# DataKeeper UI Design Specification

> 구현 기준: app/web/src/App.tsx, app/web/src/styles.css
> 최종 갱신: 2026-08-30

## 1. 디자인 목표

DataKeeper UI는 다음 세 가지 질문에 즉시 답해야 한다.

1. 이 섹션은 왜 O, S, C 중 하나로 분류됐는가?
2. 분류가 불확실할 때 사용자가 어떻게 확정·수정하고 다음 분류에 피드백하는가?
3. 현재 사용자의 접근 등급으로 어떤 섹션이 검색과 답변에 사용됐는가?

화면은 접근 결과를 **허용 또는 차단**으로만 표현한다. 권한 밖 섹션을 다른 형태로 보여주지
않으며, 분류가 확정되지 않은 섹션도 사용자 접근 화면에서 제외한다.

## 2. 공통 용어와 시각 언어

### 등급

| 코드 | 라벨 | 의미 | UI 색상 |
|---|---|---|---|
| O | Open · 공개 | 외부 공개 가능 | Green |
| S | Sensitive · 민감 | 조직 내부 제한 | Amber |
| C | Classified · 기밀 | 중대한 영향 정보 | Red |
| ? | 미정 | 아직 유효한 등급 없음 | Gray |

등급 순서는 O < S < C다. 색상만으로 의미를 전달하지 않고 코드와 한글 라벨을 함께 쓴다.

### 분류 상태

| 상태 | 사용자 라벨 | 표현 |
|---|---|---|
| auto_confirmed | 자동 판정 | 중립 상태 배지 |
| pending_review | 확인 필요 | 강조 점, 경고색, 검토 버튼 |
| user_confirmed | 사용자 확정 | 확정 상태 배지 |

pending_review는 단순한 안내가 아니라 접근 차단 상태다. 화면 설명과 도움말에서도 이 의미를
일관되게 전달한다.

### 접근 상태

- 허용: 현재 사용자의 등급 범위 안에서 확정된 원문을 조회할 수 있다.
- 차단: 등급이 높거나 분류가 미확정이어서 검색과 답변 컨텍스트에서 제외된다.
- 차단된 섹션의 제목·요약·본문을 사용자용 결과에 자리표시자로 노출하지 않는다.

## 3. 정보 구조

상단 내비게이션은 세 화면으로 구성한다.

| 메뉴 | 목적 |
|---|---|
| 분류·학습 | 문서별 섹션 분류 확인, 사용자 확정·수정, 활성 Skill 확인 |
| 권한 기반 질문 | 선택한 사용자의 접근 범위 안에서 검색·LLM 답변 |
| 판정·접근 로그 | 분류 이력과 접근 메타데이터 감사 |

    ┌──────────────────────────────────────────────────────────────┐
    │ DataKeeper       [분류·학습] [권한 기반 질문] [판정·접근 로그] │
    ├──────────────────────────────────────────────────────────────┤
    │                                                              │
    │                         현재 화면                             │
    │                                                              │
    └──────────────────────────────────────────────────────────────┘

선택 메뉴는 배경, 경계선, 텍스트 대비로 구분한다. 페이지 전환 중에도 이미 선택한 문서와 질문
사용자는 가능한 한 유지한다.

## 4. 분류·학습 화면

### 페이지 헤더

- 제목: 자동 판정부터 학습까지
- 설명: 섹션 분류, 불확실한 결과의 사용자 확인, Skill 피드백 흐름을 한 문장으로 안내한다.
- 지표: 분류 문서 수, 전체 섹션 수, 확인 필요 수, 활성 Skill 수를 표시한다.
- 확인 필요 수가 0보다 크면 경고색으로 강조한다.

### 3열 레이아웃

    ┌─────────────┬──────────────────────────────────┬──────────────┐
    │ 문서함      │ 선택 문서와 섹션 분류            │ 활성 Skill   │
    │ 업로드      │ 등급·상태·신뢰도·근거·요약       │ 피드백 목록  │
    │ 문서 목록   │ 사용자 확정·수정 편집기          │              │
    └─────────────┴──────────────────────────────────┴──────────────┘

데스크톱 기준 열 너비는 약 258px / 가변 / 270px이다. 화면이 좁아지면 Skill 열을 아래로
내리고, 모바일에서는 모든 영역을 한 열로 쌓는다.

### 문서함

- .txt, .md 파일 선택과 드래그 앤 드롭을 지원한다.
- 업로드 상태는 처리 중, 성공, 오류를 아이콘·색상·문구로 함께 보여준다.
- 문서 행은 제목, 섹션 수, 확인 필요 수 또는 판정 완료 상태, 파생 문서 등급을 표시한다.
- 삭제는 확인 대화상자를 거치며 처리 중 중복 클릭을 막는다.
- 문서 등급은 API가 섹션의 최고 등급으로 계산한 값이다. UI가 별도 값을 저장하거나 수정하지 않는다.

### 선택 문서

- 헤더에는 문서 제목과 최고 등급을 표시한다.
- 문서 등급은 섹션 중 최고 등급이라는 규칙을 고정 안내로 보여준다.
- 섹션 카드는 순번, 제목, 등급, 분류 상태, 신뢰도, 담당 범위, 판정 근거, 요약, 키워드로 구성한다.
- 분류 관리 목록에는 섹션 원문을 일괄 포함하지 않는다. 검토자가 `원문 보기`를 누른 단일
  섹션만 별도 API로 불러와 카드 안에서 펼치고 접는다.
- pending_review 카드는 경계선과 상태 배지로 강조하고 버튼 라벨을 검토하기로 표시한다.

### 사용자 확정·수정

- 섹션 카드 안에서 O/S/C 중 하나를 선택한다.
- 확정·수정 근거는 필수 입력이다.
- 저장 중에는 버튼을 비활성화하고 로딩 아이콘을 표시한다.
- 성공하면 등급·상태·문서 최고 등급·Skill 목록을 다시 불러오고 성공 메시지를 노출한다.
- 실패하면 편집 내용을 유지한 채 원인을 보여준다.
- 확정 결과와 근거가 다음 업로드 분류에 쓰이는 Skill로 축적된다는 점을 편집기 안에 명시한다.

### Skill 레일

- 활성 Skill 개수와 목록을 표시한다.
- 각 항목은 이름, 설명, 적용 등급, 키워드를 보여준다.
- Skill이 없으면 첫 사용자 확정 이후 학습이 시작된다는 빈 상태를 제공한다.
- 현재 UI는 활성 Skill만 보여준다. 전체 조회와 활성화 변경은 API에서 지원한다.

## 5. 권한 기반 질문 화면

### 사용자 선택

- 페르소나 버튼은 아바타, 이름, 부서, access_grade를 표시한다.
- 선택한 페르소나는 배경과 경계선으로 명확히 강조한다.
- 완료된 대화는 persona.id별로 브라우저 탭의 sessionStorage에 분리 저장한다. 다른 페르소나를 선택하면 해당 페르소나의 마지막 대화를 복원하고, 같은 탭을 새로고침해도 유지한다. 탭 세션이 끝나면 복원하지 않는다.
- 페르소나 전환 시 미전송 입력과 응답 대기 상태는 비운다. 전환 전에 시작한 응답이 늦게 도착해도 어느 페르소나의 대화에도 추가하지 않는다.
- 저장 당시 이름이나 access_grade가 현재 페르소나와 다르거나, 저장된 출처가 현재 등급을 넘으면 해당 대화를 폐기한다.
- 오른쪽 정책 패널은 O/S/C 각각이 현재 사용자에게 허용되는지 보여준다.
- 이 화면의 등급은 데모용 DB 페르소나에서 온다. 실제 인증·조직 권한과 연결된 것으로 오해하지
  않도록 제품 설명에 데모임을 명시한다.

### 대화 영역

- 빈 상태에는 현재 사용자 이름과 접근 등급, 추천 질문을 표시한다.
- 사용자 메시지는 오른쪽, DataKeeper 답변은 왼쪽에 둔다.
- 답변 근거는 실제 사용된 허용 섹션만 표시하며 문서명, 섹션명, 등급, 요약으로 구성한다.
- 권한 밖 또는 검수 대기 섹션은 근거 목록과 빈 상태 안내에 노출하지 않는다.
- `현재 대화 지우기`는 선택한 페르소나의 완료 대화와 진행 중 응답만 지우며 다른 페르소나의 대화에는 영향을 주지 않는다.
- 운영 정리 CLI가 서버의 채팅 세대를 교체하면 UI는 5초 주기와 창 포커스·온라인 복귀 시 이를 감지해 모든 페르소나의 저장 대화를 삭제하고 진행 중 응답을 무시한다. 최초 세대 확인 전에는 저장 대화를 표시하거나 질문을 전송하지 않는다.
- 현재 페르소나의 최근 완료 질문 최대 5개를 후속 질문 문맥으로 API에 전달한다. 과거 답변 본문은 다시 보내지 않으며 서버와 감사 로그에도 저장하지 않는다.
- 입력은 Enter로 전송하고 Shift+Enter로 줄바꿈한다.
- 사용자 미선택, 빈 입력, 전송 중에는 전송 버튼을 비활성화한다.

### LLM 폴백

- 먼저 POST /api/chat을 호출한다.
- 자격 증명 누락이나 생성 실패 시 POST /api/search를 호출해 권한 범위 내 검색 결과를 보여준다.
- 두 요청이 모두 실패하면 기술 오류를 숨기지 말고 사용자가 재시도할 수 있는 문구를 표시한다.
- 어떤 경로에서도 권한 밖 원문을 클라이언트로 보내지 않는다.

## 6. 판정·접근 로그 화면

페이지 상단에는 다음 원칙을 표시한다.

- 분류 결정 추적: 자동 판정, 사용자 확정·수정, Skill 적용
- 접근 사용 검증: 사용자 등급과 사용된 섹션 식별자·개수
- 내용 비기록: 질문, 답변, 프롬프트, 섹션 원문 제외

분류 로그와 접근 로그는 탭으로 나눈다.

### 분류 로그 행

- 시각
- 동작과 actor
- 문서·섹션 식별자
- 분류 근거
- 이전 등급과 새 등급

### 접근 로그 행

- 시각
- persona 식별자와 접근 등급
- browse, search, chat 동작
- 문서 식별자
- 허용 섹션 수와 차단 섹션 수

새로고침, 로딩, 오류, 빈 상태를 각각 제공한다. UI에서도 로그에 기록되지 않는 질문·답변을
추론해 보여주지 않는다.

## 7. 공통 상태와 피드백

| 상태 | 표현 |
|---|---|
| 초기 로딩 | 회전 아이콘과 대상 설명 |
| 저장·업로드 중 | 중복 동작 비활성화, 현재 단계 표시 |
| 성공 | 완료 아이콘과 결과 요약 |
| 검토 필요 | 경고색과 명시적 확인 필요 라벨 |
| 오류 | 원인 문구와 가능한 경우 재시도 버튼 |
| 빈 목록 | 다음 행동을 설명하는 빈 상태 카드 |
| 검색 결과 없음 | 접근 가능한 범위에서 관련 자료가 없다는 안내 |

오류와 경고를 색상만으로 구분하지 않는다. 아이콘, 라벨, 설명을 함께 사용한다.

## 8. 반응형·접근성

- 1250px 이하에서는 분류 화면의 오른쪽 Skill 영역을 전체 폭으로 내린다.
- 980px 이하에서는 질문 화면의 접근 정책 패널을 대화 아래로 이동한다.
- 720px 이하에서는 분류 화면을 한 열로 만들고 문서함·작업영역·Skill을 순서대로 배치한다.
- 키보드로 문서 선택과 질문 전송이 가능해야 한다.
- 모든 아이콘 전용 버튼에는 aria-label을 제공한다.
- 본문 대비, 포커스 표시, 상태 텍스트를 유지한다.
- 현재 구현은 라이트 테마를 기준으로 한다.

## 9. API 연결

| UI 동작 | API |
|---|---|
| 분류 문서 로드 | GET /api/classifications |
| 검수 대기만 로드 | GET /api/review-queue |
| 등급 확정·수정 | PATCH /api/sections/{section_id}/classification |
| Skill 조회·활성 상태 변경 | GET /api/skills, PATCH /api/skills/{skill_id} |
| 사용자 조회 | GET /api/personas |
| 질문 | POST /api/chat, 실패 시 POST /api/search |
| 로그 조회 | GET /api/logs/classification, GET /api/logs/access |
| 문서 업로드·삭제 | POST /api/documents/upload, DELETE /api/documents/{doc} |

## 10. 제품화 전 보안 요구

현재 UI는 해커톤 데모이며 로그인과 관리자 역할 분리가 없다. 실서비스에서는 다음을 선행한다.

- 분류 확인, Skill 변경, 로그 조회, 업로드, 삭제 API를 관리자 권한으로 보호
- persona 선택값을 신뢰하지 않고 서버 인증 주체의 접근 등급 사용
- 사용자용 응답과 관리용 분류 메타데이터의 API·캐시 경계 분리
- 감사 로그 열람 권한, 보존 기간, 내보내기 정책 정의
