# Raw Sample Documents

이 폴더는 등급 분류 대상이 되는 원문 샘플 문서만 보관한다.

- 문서 안에는 섹션별 D등급을 직접 쓰지 않는다.
- 섹션별 등급, 신뢰도, 엔티티, 검수 상태는 추후 DB에 저장하는 구조로 확장한다.
- 모든 회사명, 인명, 금액, 일정, 토큰, 계약 조건은 데모용 가상 데이터다.
- 파서는 `#` 문서 제목과 `##` 섹션 제목을 기준으로 섹션을 나눌 수 있다.

