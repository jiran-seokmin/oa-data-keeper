# DataKeeper UI Design Specification

> 구현 기준: `app/web/src/App.tsx`, `app/web/src/styles.css`
> 최종 갱신: 2026-07-10

## 1. 디자인 목표

DataKeeper UI는 문서의 보안 등급(D0~D4)과 페르소나의 접근 등급(C0~C4)이 섹션별 접근 모드(A0~A4)로 변환되는 과정을 시각적으로 설명하는 데 목적이 있다.

핵심 장면은 같은 문서를 유지한 채 페르소나를 전환했을 때 원문, 노출 제한, 일반화 요약, 엔티티 마스킹, 접근 차단 상태가 즉시 달라지는 것이다.

디자인 우선순위는 다음과 같다.

1. 현재 페르소나와 접근 등급을 항상 명확하게 표시한다.
2. 원문과 제한된 표현의 차이를 섹션 단위로 비교할 수 있게 한다.
3. D등급, A모드, gap, 판정 사유를 함께 보여준다.
4. 보안 상태는 색상뿐 아니라 코드와 텍스트 라벨로도 구분한다.

## 2. 화면 구조

애플리케이션은 고정 상단 영역과 스크롤 가능한 본문으로 구성한다.

```text
┌──────────────────────────────────────────────────────────┐
│ DataKeeper  [판정 그리드] [문서 뷰어] [매트릭스] [채팅]  │
├──────────────────────────────────────────────────────────┤
│ 페르소나  외부 고객 · 신입 · 영업팀원 · 팀장 · CEO       │
├──────────────────────────────────────────────────────────┤
│                                                          │
│                     현재 화면 본문                       │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### 상단 헤더

- 좌측에 잠금 심볼, `DataKeeper`, `접근 제어 엔진 — All Data, Safe for Everyone`을 표시한다.
- 중앙 내비게이션은 판정 그리드, 문서 뷰어, 매트릭스, 채팅 순서다.
- 우측에는 현재 페르소나의 아바타, 이름, C등급을 표시한다.
- 선택된 메뉴는 흰 배경과 약한 그림자로 구분한다.

### 페르소나 바

- C0부터 C4까지 다섯 페르소나를 가로 칩으로 배치한다.
- 각 칩은 아바타, 이름, 부서 또는 채널, C등급으로 구성한다.
- 선택된 칩은 어두운 배경과 흰 글자로 강조한다.
- 화면 폭이 부족하면 가로 스크롤을 허용한다.

### 본문

- 기본 배경은 밝은 회색이며 주요 콘텐츠는 흰색 카드에 배치한다.
- 그리드, 뷰어, 매트릭스 화면은 좌측 264px 문서 목록과 우측 콘텐츠의 2열 구조를 사용한다.
- 채팅 화면은 최대 900px 폭으로 중앙 정렬한다.

## 3. 내비게이션

| 메뉴 | 목적 |
|---|---|
| 판정 그리드 | 선택 문서의 원문·요약·키워드·D등급과 현재 페르소나 판정을 표로 비교 |
| 문서 뷰어 | 문서 흐름을 유지한 채 각 섹션의 A모드 결과를 읽기 형태로 확인 |
| 매트릭스 | 선택 문서의 섹션 × 전체 페르소나 판정을 한 화면에서 비교 |
| 채팅 | 현재 페르소나의 접근 범위를 반영한 답변과 근거 섹션 확인 |

메뉴를 전환해도 현재 페르소나와 선택 문서는 가능한 한 유지한다.

## 4. 접근 모드 표현

| 모드 | 이름 | 기본 표현 | 대표 색상 방향 |
|---|---|---|---|
| A0 | 전체 접근 | 원문 표시, 엔티티 밑줄 강조 | Green |
| A1 | 노출 제한 | 본문 블러와 `AI 추론 근거 전용` 배지 | Purple |
| A2 | 의미 제한 | 원문 대신 일반화 요약 카드 | Blue |
| A3 | 정보 마스킹 | 엔티티를 `[고객사A]`, `[금액A]` 형식으로 치환 | Orange |
| A4 | 접근 차단 | 잠금 아이콘과 차단 안내 | Gray |

### 공통 표시 규칙

- A모드 배지는 `A번호 + 이름`을 함께 표시한다.
- D등급 배지는 `D번호`와 필요한 경우 등급 이름을 함께 표시한다.
- 판정 사유에는 gap과 부서·채널 등 적용 규칙을 텍스트로 표시한다.
- 마스킹 플레이스홀더는 배경색, 글자색, 굵기를 함께 사용해 일반 본문과 구분한다.
- A4는 실제 콘텐츠 대신 잠금 상태만 표시한다.

## 5. 공통 문서 사이드바

판정 그리드, 문서 뷰어, 매트릭스는 동일한 문서 사이드바를 사용한다.

- 문서 제목과 현재 선택 상태를 표시한다.
- 문서를 선택하면 우측 콘텐츠가 해당 문서로 변경된다.
- `.txt`, `.md` 파일을 드래그 앤 드롭하거나 파일 선택으로 업로드할 수 있다.
- 업로드 상태는 읽기, 분류 중, 저장, 완료, 오류 단계로 표시한다.
- 삭제 버튼은 문서 행에 두며, 실행 전에 확인 창을 표시한다.
- 처리 중인 삭제 버튼과 업로드 입력은 중복 실행되지 않게 비활성 상태를 표현한다.

## 6. 판정 그리드

판정 그리드는 수집 결과와 현재 접근 판정을 나란히 비교하는 화면이다.

### 헤더

- 현재 페르소나 이름, C등급, 부서를 안내 문장에 표시한다.
- 우측에 A0~A4 범례를 배치한다.

### 표 구성

| 열 | 내용 |
|---|---|
| 데이터 원문 | 섹션 제목과 현재 A모드로 변환된 본문 |
| 요약 | 수집 시 생성된 A2 일반화 요약 |
| 키워드 | 검색에 사용하는 키워드 칩 |
| 보안 등급 | D등급 배지와 등급 이름 |

- 행을 선택하면 동일 문서·섹션을 유지한 채 문서 뷰어로 이동한다.
- A1은 블러와 배지, A2는 원문 비노출 안내, A3는 마스킹 본문, A4는 차단 안내로 표현한다.

## 7. 문서 뷰어

문서 뷰어는 한 문서를 읽는 흐름 안에서 섹션별 접근 결과를 보여준다.

- 상단에 문서 제목, 관련 부서, 섹션 수, 현재 페르소나를 표시한다.
- 각 섹션 헤더는 제목, D등급, A모드, 판정 사유로 구성한다.
- 그리드에서 이동한 섹션은 연한 배경과 내부 테두리로 강조한다.
- 섹션 사이에는 얇은 구분선을 사용한다.

### 섹션 본문

- A0: 원문과 엔티티 강조
- A1: 마스킹된 배경 본문을 블러 처리하고 중앙에 추론 전용 배지 표시
- A2: `일반화 요약 — 원문 비노출` 제목이 있는 요약 카드
- A3: 플레이스홀더가 강조된 마스킹 본문과 치환 안내
- A4: 회색 배경의 잠금 안내

## 8. 판정 매트릭스

판정 매트릭스는 선택 문서의 모든 섹션과 다섯 페르소나 조합을 비교한다.

- 첫 번째 열에는 섹션 제목과 D등급을 표시한다.
- 이후 열에는 페르소나 아바타, 이름, C등급을 표시한다.
- 각 셀은 A모드 배지로 판정 결과를 표시한다.
- 셀의 도움말에는 모드 이름과 판정 사유를 제공한다.
- 페르소나 헤더나 셀을 선택하면 해당 페르소나로 전환한다.
- 현재 페르소나 열은 연한 배경으로 강조한다.

## 9. 채팅

### 빈 상태

- 중앙에 DataKeeper 심볼, 제목, 현재 페르소나 안내를 표시한다.
- 입력창 아래에 대표 질문을 추천 칩으로 제공한다.

### 대화 상태

- 사용자 메시지는 우측의 어두운 말풍선으로 표시한다.
- DataKeeper 답변은 좌측의 흰 말풍선과 심볼로 표시한다.
- 답변은 Markdown 문단, 목록, 코드, 링크를 지원한다.
- 답변 아래에는 최대 3개의 근거 섹션과 해당 D등급·A모드를 표시한다.
- 출력 가드가 재생성 또는 차단되면 별도의 경고 문구를 표시한다.
- LLM 호출 실패 후 조회 모드로 전환되면 폴백 상태를 표시한다.
- 응답 대기 중에도 사용자 질문을 먼저 표시하고 `답변 판정 중…` 상태를 보여준다.

### 입력창

- Enter는 전송, Shift+Enter는 줄바꿈으로 동작한다.
- 전송 중이거나 입력이 비어 있으면 전송 버튼을 비활성화한다.
- 현재 C등급과 근거 섹션 제공 여부를 입력창 하단 칩으로 안내한다.

## 10. 상태와 피드백

| 상태 | 표현 |
|---|---|
| 로딩 | 회전 아이콘 또는 `답변 판정 중…` 텍스트 |
| 업로드 진행 | 현재 단계, 파일명, 설명 |
| 업로드 완료 | 완료 아이콘, 섹션 수, 최고 D등급, 검수 대상 수 |
| 오류 | 붉은 계열 배경·경계선·오류 문구 |
| 빈 문서 목록 | 표시할 문서가 없다는 안내 카드 |
| 검색 결과 없음 | 접근 범위 안에서 관련 정보를 찾지 못했다는 안내 |

오류 메시지는 작업 대상과 실패 원인을 함께 표시하고, 성공·경고·오류는 색상과 아이콘을 같이 사용한다.

## 11. 시각 스타일

### 색상

- 앱 배경: `#F9FAFB`
- 카드·헤더: `#FFFFFF`
- 주요 텍스트: `#111827`
- 본문 텍스트: `#384252`
- 보조 텍스트: `#4B5563`, `#9CA3AF`
- 경계선: `#E5E7EB`

상태색은 A모드와 D등급 의미를 유지하는 범위에서만 사용하며, 일반 레이아웃은 무채색 중심으로 구성한다.

### 형태와 간격

- 카드와 컨트롤은 5~8px의 작은 모서리 반경을 기본으로 한다.
- 카드 그림자는 약하게 사용해 정보 계층만 구분한다.
- 본문 기본 글자 크기는 13px 전후, 문서 제목은 19px 전후다.
- 상단 헤더 높이는 64px, 문서 사이드바 기준 폭은 264px다.

### 아이콘

아이콘은 `lucide-react`를 사용한다. 잠금, 파일, 업로드, 삭제, 정보, 완료, 로딩, 전송 의미를 텍스트와 함께 표현한다.

## 12. 반응형과 테마

- 페르소나 바는 좁은 화면에서 가로 스크롤한다.
- 설명 영역과 범례는 공간이 부족하면 줄바꿈한다.
- 문서 표는 각 열의 최소 폭을 유지하며 본문 영역에서 스크롤한다.
- 현재 구현은 라이트 테마만 제공한다. 다크 테마 토글과 시스템 테마 연동은 구현되어 있지 않다.
