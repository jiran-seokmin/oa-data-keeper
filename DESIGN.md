# DataKeeper UI Design Specification

> 현행 구현 기준: React 19 + TypeScript + Vite SPA (`app/web`) · FastAPI (`app/server.py`)
> 최종 갱신: 2026-07-10

## 1. 제품 개요

DataKeeper는 데이터 보안 등급(D0~D4), 사용자 접근 등급(C0~C4), 상황(Context)을 조합해 문서의 섹션마다 A0~A4 접근 모드를 판정하는 데모 애플리케이션이다.

핵심 경험은 같은 문서를 유지한 채 페르소나를 전환하면 각 섹션의 원문·요약·마스킹·노출 제한·차단 상태가 즉시 달라지는 것이다. 접근 판정은 LLM이 아니라 `app/engine.py`의 결정론적 규칙으로 수행한다.

## 2. 구현 범위

### 현재 구현

- 판정 그리드: 선택한 문서의 섹션별 D등급과 현재 페르소나의 A모드 표시
- 문서 뷰어: A0 원문, A1 노출 제한, A2 일반화 요약, A3 엔티티 마스킹, A4 잠금 렌더
- 판정 매트릭스: 섹션 × 5개 페르소나의 A모드 히트맵
- 채팅: `/api/chat` 우선 호출, LLM 자격 증명 또는 호출 실패 시 `/api/search`로 폴백
- 문서 업로드: `.txt`/`.md` 파일을 Gemini로 분류해 SQLite에 저장
- 문서 삭제: 문서·섹션·엔티티 메타데이터 삭제

### 현재 미구현

- 로그인, SSO, 조직도 기반 실제 사용자 인증·인가
- 판단/집계 목적 전환 UI(백엔드와 엔진은 `purpose`를 지원하지만 웹은 현재 `info`로 고정)
- 감사 로그 저장/API/화면
- 관리자 검수 큐와 정책 편집 UI
- 프런트 단위·E2E 테스트

## 3. 정보 구조

상단 내비게이션은 다음 네 화면으로 구성한다.

1. 판정 그리드
2. 문서 뷰어
3. 매트릭스
4. 채팅

상단의 페르소나 바에서 외부 고객(C0), 신입 개발자(C1), 영업팀원(C2), 영업팀장(C3), CEO(C4)를 전환한다. 이는 보안 인증 UI가 아니라 접근 결과를 비교하기 위한 데모 컨트롤이다.

## 4. 접근 모드 표현

| 모드 | 이름 | UI 표현 |
|---|---|---|
| A0 | 전체 접근 | 원문과 엔티티 하이라이트 |
| A1 | 노출 제한 | 블러 처리된 배경과 `AI 추론 근거 전용` 배지 |
| A2 | 의미 제한 | 원문 대신 일반화 요약 카드 |
| A3 | 정보 마스킹 | 엔티티를 타입 플레이스홀더로 치환 |
| A4 | 접근 차단 | 잠금 아이콘과 차단 안내 |

각 섹션에는 D등급, A모드, gap 및 판정 사유를 함께 표시한다. 매트릭스에서는 A0~A4 색상 배지로 페르소나별 차이를 비교한다.

## 5. 화면별 동작

### 판정 그리드

- 좌측 문서 목록에서 문서를 선택한다.
- 원문/판정 결과, A2 요약, 키워드, D등급을 표 형태로 보여준다.
- 행을 선택하면 해당 섹션에 초점을 둔 문서 뷰어로 이동한다.

### 문서 뷰어

- 선택 문서의 모든 섹션을 현재 페르소나 기준으로 렌더한다.
- 섹션 제목 옆에 D등급, A모드, 판정 사유를 표시한다.
- 페르소나 변경 시 `/api/sections`를 다시 조회한다.

### 매트릭스

- 선택 문서의 섹션을 행, 페르소나를 열로 표시한다.
- 셀은 해당 조합의 A모드를 나타낸다.
- 열 또는 셀을 선택하면 현재 페르소나가 바뀐다.

### 채팅

- 질문을 보내면 `/api/chat`을 먼저 호출한다.
- LLM 호출에 실패하면 `/api/search` 결과를 근거로 조회 모드 안내를 표시한다.
- 현재 UI는 사용 섹션의 제목·D등급·A모드를 최대 3개 출처 칩으로 표시한다.
- 페르소나 변경은 기존 대화를 자동 재실행하지 않는다.

### 업로드와 삭제

- 문서 목록에서 `.txt` 또는 `.md` 파일을 드래그 앤 드롭한다.
- 업로드 분류는 Gemini 전용이며 API 키가 필요하다.
- 삭제 전 브라우저 확인 창을 표시하고 관련 섹션·엔티티를 함께 삭제한다.

## 6. 시각 스타일

- 배경: 밝은 회색, 카드: 흰색, 본문: 차콜 계열
- 경계선과 그림자는 약하게 사용해 판정 배지에 시선이 모이게 한다.
- A모드와 D등급은 일관된 색상·라벨로 표현한다.
- 반응형에서는 문서 목록과 본문이 읽을 수 있는 최소 폭을 유지한다.

실제 스타일의 기준은 `app/web/src/styles.css`, 화면 구조의 기준은 `app/web/src/App.tsx`다.

## 7. 기술 구조

```text
React SPA
  ├─ GET /api/personas
  ├─ GET /api/sections?persona_id=
  ├─ GET /api/matrix
  ├─ POST /api/chat → 실패 시 POST /api/search
  ├─ POST /api/documents/upload
  └─ DELETE /api/documents/{doc}
          │
       FastAPI
          │
  SQLite + 결정론적 판정 엔진
```

Streamlit `app/ui.py`는 기존 렌더링 규칙을 보존한 레거시 데모다. 현재 기본 웹 UI와 제품 디자인 기준은 `app/web`의 React 애플리케이션이다.

## 8. 보안 경계

현재 페르소나 선택은 데모 기능이며 인증을 대신하지 않는다. 따라서 이 UI와 API를 실서비스 접근제어 경계로 간주해서는 안 된다. 실서비스화에는 검증된 사용자 컨텍스트, API 인가, 관리자 기능 보호, A4 메타데이터 비공개와 감사 기능이 별도로 필요하다.
